specVersion: "v1beta"
serviceId: "smartmeter"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartmeter-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
